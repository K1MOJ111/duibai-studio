# 当前验证：Pro 局部建议已上线，线上关键路径通过

## 提交核验 · 2026-09-11

- 已在原飞书题目的“你的提交”下方空白块填入产品、Git 仓库、最终录屏链接、体验说明、取舍与能力边界、约 3 小时实际投入（候选人估算）。
- 页面显示“已经保存到云端”；重新加载后逐段检查，所有提交内容均存在，写入前采集的 30 处原有文本均未缺失。核验结果保存在本地 `output/feishu-submission-verification.json`。
- 最终录屏为 `duibai-demo-final.mp4`，213.28 秒，补充投入时间后已检查字幕画面。GitHub 文件与本地最终文件 SHA-256 一致，下载链接返回 200。
- HR 通知尚未发送，不能将页面提交等同于已通知招聘方。

## 线上验证 · 2026-09-11

- 正式地址：https://duibai-studio.vercel.app 。Vercel 部署 `dpl_2oWYjTrKyF2CMDJ9Lq4xNP9KwGaS` 状态为 READY，通过 CLI 发布，尚未绑定 GitHub 自动部署。
- 部署前实际文件清单为 10 个文件、45,862 字节；环境文件、本地录屏、历史提示词和调试输出均被排除。模型密钥与体验码在平台配置为 Secret，不在仓库或前端资源中。
- 不带 Vercel 登录状态访问：首页与健康检查均为 200；健康检查显示 ready 和 accessRequired 为 true。`/.env`、`/.env.local`、`/rewrite-prompt.txt`、`/evaluation.json` 均为 404，无体验码 POST 为 401。
- 从线上网页加载 N4 原稿，真实请求返回 200，耗时 60.538 秒，得到 3 条建议；保留父亲怕被笑、愿意请教但不要代做、慢讲留时间的意思。本次输入 820、输出 4,072 tokens；响应存于本地忽略目录 `output/online-N4-response.json`。
- 浏览器取消第一条、采用其余两条、手动修改、导出，然后撤销。下载正文已逐字核对，等于所选建议加手动编辑；撤销恢复原稿。
- 桌面与 390×844 手机布局截图已查看，无横向溢出。浏览器控制台 0 错误、0 警告。

下面是此前本地验证与失败记录；一次线上重复不构成通用质量或可用率保证。

## 本地验证 · 2026-09-10

下面保留早期失败记录，当前本地版本以本节为准。

## 当前效果与范围

采用 `deepseek-v4-pro`、high 思考强度、8,192 输出上限，一次生成局部替换，不再让模型重写整份正文。六段原创小样与模型原始建议、用量、耗时见 `evaluation.json`。

| 场景 | 模型建议数 | 最终提供数 | 审阅结果 |
|---|---:|---:|---|
| S2 旧缝纫机 | 5 | 5 | 保留分担与参与决定两个诉求，欠款/未成交/明早商量/中午答复保持；部分表达仍偏保守 |
| N2 仓库试扫码 | 3 | 3 | 保留只试这一车、未认责和后续未承诺；有局部口语改善 |
| N1 排练室钥匙 | 4 | 4 | 保留今晚九点及后续权限边界；未新增成功承诺 |
| N3 裁缝铺 | 3 | 2 | 一条把“不是一定要追责”改成“不是要追责”，字面保护保留原句；其余两条保留原因未明与提前通知的意思 |
| N4 夜校作业 | 3 | 3 | 保留父亲的羞怯、愿意求助但不代做、慢讲与暂不告诉母亲 |
| S3 雨伞 | 0 | 0 | 自然的父女对白原样保留 |

N3/N4 在这套改写配置首次调用前已写好，没有在该配置的提示词中放入对应改稿答案；但它们仍由当前 Codex 编写并审阅，不是独立真实用户测试。N3 的保护规则是发现问题后加入的保守修复，并非预先通过的盲测结果。六段不构成通用成功率，也不证明“完全去 AI 味”。

六次请求累计 243.134 秒，单次 15.501—62.095 秒；5,132 输入 tokens、15,037 输出 tokens，缓存输入 2,048 tokens。按请求所在高峰/空闲时段与官方人民币单价估算 0.294626 元，未核对账单。请求横跨额度恢复，日历间隔不能当成开发投入。

## 当前浏览器与工程证据

- `npm test`：5 组离线检查通过，包括坏行号、重复修改、伪造原文、非对白修改、非法内容、限定词保留、密钥与鉴权、并发占用和异常后释放。模型响应为明确模拟数据。
- 将六份真实响应送入生产 `applyModelEdits`：获得上表的保留/拦截数，不修改场景、动作或角色。
- 浏览器真实导入 S2 TXT，再调用服务端 Pro：69.419 秒、5 条建议。再次保留两个诉求和原时间安排，原始浏览器响应保存在被忽略的 `output/live-S2-pro-patches.json`。一次重复不足以宣称稳定成功率。
- 录屏时再次真实调用 N4：73.833 秒、3 条建议。保留父亲的羞怯、愿意请教且不代做、慢讲留时间；取消第一条、采用另两条并手改，录屏下载结果也已逐字核对。记录在 `output/live-N4-recording.json`，不属于前面六次样例调用的费用统计。
- 浏览器取消第一条建议、采用其余四条、修改最后一句、导出 TXT；下载字节解码后与“所选改稿＋手动编辑”完全一致。之后撤销，正文恢复为采用前原稿。
- 390×844 手机视口和 1440×1050 桌面视口检查，无横向溢出，已查看输入页与对照页截图。
- 浏览器模拟 504：已存在的正文和改稿保留、按钮恢复；模拟仅用于故障测试，测试后移除拦截。控制台此时出现的 504 日志是预期故障；真实成功路径检查时没有控制台错误。
- 本地 HTTP：`/.env`、`/rewrite-prompt.txt`、`/evaluation.json` 均返回 404，健康检查返回 200，不暴露密钥。线上还需独立验证部署、函数时限、访问码及外网可访问性。

当前是可体验、可审阅的辅助改稿切片。字面限定词保护不是语义证明；模型建议仍需要作者结合上下文确认。没有缓存改稿、自动评分或“已保证原意”的标记。

---

# 历史记录：早期网页集成失败

2026-09-09。按“方向不行再暂停”的要求，当前暂停 AI 处理方案及上线准备。保留网页和所有结果，不用界面完成度替代改稿质量。这里没有证明“台词精修”需求无价值，也没有证明 DeepSeek 永远不能完成；失败的是本轮实测的“完整历史 Prompt + 单次模型改稿”配置。

## 已完成和核验

- 原生网页及 Node.js 后端：整场粘贴、UTF-8 TXT 导入、背景选填、真实模型调用、逐句比较、整场对读、选择采用、继续编辑、一次撤销、复制与导出。
- 1440 × 1050 的 Chrome 桌面页面已实际查看。真实链路走通 N1 示例：8 句对白、3 处建议、模型调用约 2.568 秒。没有用缓存或 mock 冒充生成。
- 实际取消第 6 行建议，采用其余两处，手改末句后通过浏览器下载 TXT。下载文件与预期完整正文逐字一致，未采用的原句保留，手改内容包含在文件中。
- 导入 S2 原创场景成功，不填写补充背景，进行两种模式实测。原稿自身包含两千四、明天中午答复等信息。
- `npm test` 的 4 组检查通过，包括文本合并、冻结行、角色前缀、非对白标题、时间行、输入上限、越界与伪造响应、API 鉴权、超时、并发占用及失败后释放。使用模拟响应的部分仅用于工程检查。
- 真实浏览器在上述运行中未记录 JS 错误。它不等于所有浏览器与状态已覆盖。

## 三次真实模型调用

| 输入 | 配置 | 耗时 | 输入 / 输出 tokens | 结果 |
|---|---|---:|---:|---|
| N1 借排练室，含原有背景 | Flash，非思考，temperature 0.5，输出上限 6000 | 2.568 秒 | 3817 / 192 | 3 处建议，书面表达变口语；增加了“后面给你信儿”的回应约定，不作为严格语义无变化的通过样例 |
| S2 旧缝纫机，不额外填背景 | 同上 | 2.253 秒 | 3809 / 219 | 新增“下个月”，删掉主动分担与参与决定诉求，不通过 |
| 同一 S2，输入与 Prompt 不变 | Flash，thinking enabled，reasoning_effort low，输出上限 8192，无 temperature | 8.678 秒 | 3809 / 729（思考 460） | 新增机器闲置等信息，诉求仍变化，不通过 |

前两次输出文件未记录缓存用量，不能据此给出精确计费。三次所有输入均按高峰未缓存价计算，费用估算上界为 0.044565 元，未查询账单。[官方价格](https://api-docs.deepseek.com/zh-cn/quick_start/pricing/)。本轮总计 3 次真实请求，没有在失败后继续追加实验。

## 为什么暂停

S2 原句：

> 我也希望能够承担家庭责任，而不是所有事情都由你来决定。

快速模式：

> 跟你说，你肯定又说不卖。

思考模式：

> 我不是不重视它，我是觉得咱俩谁决定了都一样。你还不是什么事都自己扛着，从来不听我说。

从“我要分担，也要参与决定”变成了“谁决定都一样”，不能只当作口气差别。思考模式还新增“这机器搁那儿也没人用”，并把接受共同面对问题改成“你要是早点跟我说这些，我也不至于去动它”。这些变化会影响人物动机与责任归因。

程序保护了场景、动作和发言人，无法据此识别这些对白语义问题。继续堆页面功能、把建议标为“已检查”，或者只展示较好的场景，都不能解决核心问题。因此当前不推荐将这个版本作为完成品提交。

## 保留证据与待做事项

本地 `output/live-N1.json`、`output/live-S2-fast.json`、`output/live-S2-thinking.json` 保存实际 API 返回；`output/export-edited.txt` 是浏览器真实下载的正文；`output/S2.txt` 是原创测试输入。这些文件被 Git 忽略，不含密钥。历史 Prompt 原文哈希仍为 `2a027980008153d6241600747748ce13d3c82b3a5e2a5dee6c215ba57dd27a2b`。

待做：确定能达到最低改稿质量的 AI 处理方法；再完成手机端和完整异常交互验证；复验更新后的整场对读；实际部署、公开 Git 仓库、汇报录屏和提交。当前均未完成，未发送外部消息。

工程本轮可核对的时段覆盖北京时间 16:58 至 17:18，之前还有入口读取，之后还有结果归档；不将这段时长冒充整个项目投入。此前探索另有记录，本文件没有重算全部实际工时或对外申报工时。
